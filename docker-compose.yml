services:
  finnhub-mock:
    build: .
    image: finnhub-stock-prices-mock-server:latest
    container_name: finnhub-stock-prices-mock-server
    ports:
      - "8765:8765"
    command: ["--host", "0.0.0.0", "--port", "8765", "--interval", "1.0"]
    restart: unless-stopped
